// Minimal TypeScript representation of package-lock.json for developer reference
const lock = {
  name: "pelada-final",
  lockfileVersion: 3,
  requires: true,
  // For full lockfile, refer to package-lock.json; this file is a lightweight TS mirror.
}

export default lock
